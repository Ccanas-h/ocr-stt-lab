diff --git a/node_modules/@capgo/capacitor-speech-recognition/android/src/main/java/app/capgo/speechrecognition/SpeechRecognitionPlugin.java b/node_modules/@capgo/capacitor-speech-recognition/android/src/main/java/app/capgo/speechrecognition/SpeechRecognitionPlugin.java
index 37e1561..91a0ee4 100644
--- a/node_modules/@capgo/capacitor-speech-recognition/android/src/main/java/app/capgo/speechrecognition/SpeechRecognitionPlugin.java
+++ b/node_modules/@capgo/capacitor-speech-recognition/android/src/main/java/app/capgo/speechrecognition/SpeechRecognitionPlugin.java
@@ -111,6 +111,64 @@ public class SpeechRecognitionPlugin extends Plugin implements Constants {
 
     @PluginMethod
     public void isOnDeviceRecognitionAvailable(PluginCall call) {
+        // createOnDeviceSpeechRecognizer() exige el hilo principal y lanza
+        // RuntimeException si se llama desde otro. Capacitor ejecuta los
+        // @PluginMethod en un hilo de fondo, asi que sin este salto la app se
+        // cae con un crash nativo que JavaScript no puede atrapar.
+        bridge.getActivity().runOnUiThread(() -> isOnDeviceRecognitionAvailableOnMainThread(call));
+    }
+
+    /**
+     * Pide a Android que descargue el modelo de reconocimiento local.
+     *
+     * Usa deliberadamente la sobrecarga de un solo argumento de
+     * triggerModelDownload(), disponible desde API 33. La sobrecarga con
+     * ModelDownloadListener es API 34, y referenciar esa clase en un
+     * dispositivo API 33 revienta con ClassNotFoundException al cargar el
+     * plugin. Sin callback no hay progreso: la app vuelve a consultar el
+     * estado con isOnDeviceRecognitionAvailable().
+     */
+    @PluginMethod
+    public void downloadOnDeviceModel(PluginCall call) {
+        bridge.getActivity().runOnUiThread(() -> downloadOnDeviceModelOnMainThread(call));
+    }
+
+    private void downloadOnDeviceModelOnMainThread(PluginCall call) {
+        String language = call.getString("language", Locale.getDefault().toLanguageTag());
+
+        if (!canUseOnDeviceRecognition()) {
+            call.resolve(
+                new JSObject().put("state", "unsupported").put("message", "Esta version de Android no admite reconocimiento local.")
+            );
+            return;
+        }
+
+        final SpeechRecognizer downloader;
+        try {
+            downloader = SpeechRecognizer.createOnDeviceSpeechRecognizer(bridge.getActivity());
+        } catch (RuntimeException ex) {
+            call.resolve(
+                new JSObject().put("state", "unsupported").put("message", "El dispositivo no expone un reconocedor local.")
+            );
+            return;
+        }
+
+        try {
+            Intent intent = buildRecognizerIntent(language, MAX_RESULTS, null, false, 0, true);
+            downloader.triggerModelDownload(intent);
+            call.resolve(
+                new JSObject()
+                    .put("state", "downloading")
+                    .put("message", "Descarga solicitada al sistema. Puede tardar unos minutos; vuelve a comprobar.")
+            );
+        } catch (RuntimeException ex) {
+            call.resolve(new JSObject().put("state", "unsupported").put("message", String.valueOf(ex.getMessage())));
+        } finally {
+            downloader.destroy();
+        }
+    }
+
+    private void isOnDeviceRecognitionAvailableOnMainThread(PluginCall call) {
         String language = call.getString("language", Locale.getDefault().toLanguageTag());
         if (!canUseOnDeviceRecognition()) {
             call.resolve(new JSObject().put("available", false));
@@ -120,7 +178,7 @@ public class SpeechRecognitionPlugin extends Plugin implements Constants {
         final SpeechRecognizer supportChecker;
         try {
             supportChecker = SpeechRecognizer.createOnDeviceSpeechRecognizer(bridge.getActivity());
-        } catch (UnsupportedOperationException ex) {
+        } catch (RuntimeException ex) {
             call.resolve(new JSObject().put("available", false));
             return;
         }
@@ -701,6 +759,20 @@ public class SpeechRecognitionPlugin extends Plugin implements Constants {
         long currentSessionId,
         boolean restarting
     ) {
+        // ModelDownloadListener es API 34. En API 33 la clase no existe y
+        // referenciarla revienta el proceso, asi que ahi se usa la variante
+        // sin callback y se avisa al llamador de que reintente.
+        if (android.os.Build.VERSION.SDK_INT < 34) {
+            speechRecognizer.triggerModelDownload(intent);
+            String message = "On-device speech model download was requested. Try again once it finishes.";
+            if (call != null) {
+                call.reject(message);
+            }
+            emitErrorEvent("MODEL_DOWNLOAD_SCHEDULED", message, currentSessionId);
+            finishSession(currentSessionId, "error", "MODEL_DOWNLOAD_SCHEDULED");
+            return;
+        }
+
         speechRecognizer.triggerModelDownload(
             intent,
             mainExecutor(),
@@ -1100,6 +1172,13 @@ public class SpeechRecognitionPlugin extends Plugin implements Constants {
             } finally {
                 lock.unlock();
             }
+            // Android avisa aqui, y solo aqui, que el microfono ya esta
+            // capturando. El evento 'started' del plugin se emite antes, justo
+            // despues de startListening(), asi que no sirve para saber cuando
+            // el usuario puede hablar. Sin esta senal, quien use el plugin
+            // muestra "habla ahora" demasiado pronto y pierde la primera
+            // palabra.
+            notifyListeners("readyForSpeech", new JSObject());
         }
 
         @Override
diff --git a/node_modules/@capgo/capacitor-speech-recognition/ios/Sources/SpeechRecognitionPlugin/SpeechRecognitionPlugin.swift b/node_modules/@capgo/capacitor-speech-recognition/ios/Sources/SpeechRecognitionPlugin/SpeechRecognitionPlugin.swift
index afe6c84..759334a 100644
--- a/node_modules/@capgo/capacitor-speech-recognition/ios/Sources/SpeechRecognitionPlugin/SpeechRecognitionPlugin.swift
+++ b/node_modules/@capgo/capacitor-speech-recognition/ios/Sources/SpeechRecognitionPlugin/SpeechRecognitionPlugin.swift
@@ -102,7 +102,8 @@ public final class SpeechRecognitionPlugin: CAPPlugin, CAPBridgedPlugin {
             addPunctuation: call.getBool("addPunctuation") ?? false,
             contextualStrings: contextualStrings,
             useOnDeviceRecognition: call.getBool("useOnDeviceRecognition") ?? false,
-            continuousPTT: call.getBool("continuousPTT") ?? false
+            continuousPTT: call.getBool("continuousPTT") ?? false,
+            forceLegacyRecognizer: call.getBool("forceLegacyRecognizer") ?? false
         )
 
         if options.continuousPTT && !options.partialResults {
@@ -279,6 +280,7 @@ public final class SpeechRecognitionPlugin: CAPPlugin, CAPBridgedPlugin {
             let locale = Locale(identifier: options.language)
             if #available(iOS 26.0, *),
                options.useOnDeviceRecognition,
+               !options.forceLegacyRecognizer,
                await SpeechAnalyzerRecognitionSupport.supports(locale: locale) {
                 beginModernRecognition(call: call, options: options, locale: locale, sessionId: sessionId, restarting: restarting)
             } else {
@@ -334,6 +336,12 @@ public final class SpeechRecognitionPlugin: CAPPlugin, CAPBridgedPlugin {
         if #available(iOS 16.0, *) {
             recognitionRequest.addsPunctuation = options.addPunctuation
         }
+        // Sin esta línea `SFSpeechRecognizer` manda **siempre** el audio a los
+        // servidores de Apple. Apple la ofrece desde iOS 13; el plugin nunca la
+        // asignaba, así que por debajo de iOS 26 no existía modo sin conexión.
+        if options.useOnDeviceRecognition {
+            recognitionRequest.requiresOnDeviceRecognition = recognizer.supportsOnDeviceRecognition
+        }
         self.recognitionRequest = recognitionRequest
 
         let inputNode = audioEngine.inputNode
@@ -348,6 +356,10 @@ public final class SpeechRecognitionPlugin: CAPPlugin, CAPBridgedPlugin {
 
         do {
             try audioEngine.start()
+            // Instante real en que el micrófono ya captura, equivalente a
+            // `onReadyForSpeech` en Android. Se emite con el mismo nombre para
+            // que la app mida la ventana de arranque igual en ambas plataformas.
+            notifyListeners("readyForSpeech", data: ["sessionId": sessionId])
             emitListeningState(state: "started", sessionId: sessionId, reason: restarting ? .results : .userStart, status: "started")
         } catch {
             call?.reject("Unable to start audio engine: \(error.localizedDescription)")
@@ -432,6 +444,7 @@ public final class SpeechRecognitionPlugin: CAPPlugin, CAPBridgedPlugin {
                 return
             }
 
+            self.notifyListeners("readyForSpeech", data: ["sessionId": sessionId])
             self.emitListeningState(state: "started", sessionId: sessionId, reason: restarting ? .results : .userStart, status: "started")
         }
 
@@ -855,4 +868,10 @@ private struct RecognitionOptions {
     let contextualStrings: [String]
     let useOnDeviceRecognition: Bool
     let continuousPTT: Bool
+    /// Fuerza el camino `SFSpeechRecognizer` aunque el equipo tenga iOS 26.
+    ///
+    /// Sin esto, `useOnDeviceRecognition` en iOS 26 siempre deriva a
+    /// `SpeechAnalyzer`, y las dos rutas locales de Apple quedan imposibles de
+    /// medir por separado.
+    let forceLegacyRecognizer: Bool
 }
